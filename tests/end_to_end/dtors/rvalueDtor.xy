import libxy.stdio;
import libxy.string;

struct Data {
    num: Int,
}

func dtor(s: Data) {
    print(f"Destroying {s.num}\n");
}

func makeStr(num := 0) -> Data {
    return Data{num};
}

func makeStrMacro() Data{10};


func fun(s1: mut Data, s2: Data, s3: Data) {
    print("In func()\n");
    # ...
}

func main~EntryPoint() {
    s := 'makeStr;
    fun(makeStr(1), 'makeStrMacro, Data{20});
}