import libxy.string;
import libxy.slice;
import libxy.stdio;
import libxy.memory;

func test(cond: Bool) {
   if (cond) {
        print("Match\n");
    } else {
        print("No Match\n");
    }
}

func funcReturningIdx(s: String) -> s[ RangeSize ] {
    print("In funcReturningIdx\n");
    res := 0uz:6uz;
    return res;
}

func get(s: String, int: RangeSize) -> String
>> int.start < int.end
{
    return str(s'memory.addr + int.start, int.end - int.start, false);
}

func returnString() -> String {
    print("In returnString\n");
    return f"partialString";
}

func main~EntryPoint() {
    test(funcReturningIdx("stringFull") == "string");
    test(funcReturningIdx(returnString()) == "string");
}