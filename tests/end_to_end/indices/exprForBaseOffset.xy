import libxy.string;
import libxy.slice;
import libxy.stdio;
import libxy.memory;

func test(cond: Bool) {
   if (cond) {
        print("Match\n");
    } else {
        print("No Match\n");
    }
}

func funcReturningIdx(s: Str) -> s[ RangeSize ] {
    print("In funcReturningIdx\n");
    res := 0uz:(s.size-4);
    return res;
}

func get(s: Str, int: RangeSize) -> Str~MemExt
>> int.start < s.size
>> (int.end - int.start) <= s.size
{
    return Str~MemExt{s.addr + int.start, int.end - int.start};
}

func returnString() -> Str~MemManaged {
    print("In returnString\n");
    return f"stringPartial";
}

func main~EntryPoint() {
    test(funcReturningIdx("stringFull") == "string");
    test(funcReturningIdx(returnString()) == "string");
}