import libxy.string;
import libxy.slice;
import libxy.stdio;
import libxy.memory;

func test(cond: Bool) {
   if (cond) {
        print("Match\n");
    } else {
        print("No Match\n");
    }
}

func funcReturningIdx(s: Str) -> s[ RangeSize ] {
    print("In funcReturningIdx\n");
    res := 0uz:6uz;
    return res;
}

func get(s: Str, int: RangeSize) -> Str
>> int.start < int.end
{
    return str(s.addr + int.start, int.end - int.start, false);
}

func returnString() -> Str {
    print("In returnString\n");
    return f"partialString";
}

func main~EntryPoint() {
    test(funcReturningIdx("stringFull") == "string");
    test(funcReturningIdx(returnString()) == "string");
}