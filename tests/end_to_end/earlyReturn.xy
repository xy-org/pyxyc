func main~EntryPoint() {
    test();
}

func test() {
    a : mut = 0;
    b : mut = 10;
    while (a < b) {
        a++;
        b = b - a;
        if (a >= b) {
            return;
        }
    }
}