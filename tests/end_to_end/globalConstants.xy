import libxy.stdio;
import libxy.string;
import libxy.unit;

c := 299792458~mps;
pi := 3.1415;

def main~EntryPoint() -> int {
    print(f"Speed of Light = {c} m/s\n");
    print(f"Pi = {pi, precision=2}\n");
    return 0;
}
