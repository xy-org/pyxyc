import libxy.stdio;
import libxy.string;

func compute() -> Int {
    print("In compute\n");
    return 42;
}

func len() -> Int {
    print("In len\n");
    return 10;
}

struct Slice {
    begin: Int;
    end: Int;
}

func slice(begin: Int, end: Int) = Slice{begin, end};

func main~EntryPoint() {
    s0 := compute() +: len();
    print(f"Slice {s0.begin} {s0.end}\n");
    s1 := s0.end +: 10;
    print(f"Slice {s1.begin} {s1.end}\n");
}