import libxy.(string, memory);
import libxy.stdio;

func test~EntryPoint() {
    str : String;
    a := 0;
    b := 10;
    if (a > b) {
        str = f"{a}{b}";
    } else {
        str = f"{b}{a}";
    }
    print(str);
}