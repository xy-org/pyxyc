import libxy.stdio;
import libxy.string;
import libxy.memory;

struct Data {
    num: Int;
}

*func fun(p1: String) -> Data {
    return retagged(p1~[memory=MemStatic]);
}

*func retagged(p2: String) -> Data
>> isNullTerminated(p2)
{
    # ...
    return Data{};
}

func main~EntryPoint() {
    _ = fun("abc");
    _ = fun(f"abc");
}