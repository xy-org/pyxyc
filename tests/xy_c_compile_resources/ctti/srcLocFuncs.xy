import xy.ctti;

func str~[StrCtor{prefix=""}](addr: Ptr, size: Usize) addr;

func testFunc() {
}

func test() {
    a := $testFunc()'nameof;
    b := $testFunc()'packageof;
    c := packageof(testFunc());
    d := $testFunc()'fileof;
    e := $testFunc()'linenoof;
    f := linenoof(1 + 1);
    g := 'fileof;
    h := 'linenoof;
    i := (1 * 10 + 20)'srcof;
    j := (1 * 10 + 20)'srclineof;
    k := 'srclineof;
}