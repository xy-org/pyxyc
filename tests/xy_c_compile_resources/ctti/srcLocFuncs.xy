import xy.ctti;

def str~[StrCtor{prefix=""}](addr: Ptr, size: Size) addr;

def testFunc() {
}

def test() {
    a := $testFunc()'nameof;
    b := $testFunc()'packageof;
    c := packageof(testFunc());
    d := $testFunc()'fileof;
    e := $testFunc()'linenoof;
    f := linenoof(1 + 1);
    g := 'fileof;
    h := 'linenoof;
}