;; Test arithmatic with Longs

func test1(x: Long, y: Long) -> Long {
    a := x * y;
    b := x / y;
    return a \mulhi b;
}

func test1(x: Ulong, y: Ulong) -> Ulong {
    a := x * y;
    b := x / y;
    return a \mulhi b;
}
