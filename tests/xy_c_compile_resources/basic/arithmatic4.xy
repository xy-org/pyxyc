;; Test pow(er) operator

func test(x: Int, y: Int) -> Int {
    a := x ^ 2;
    b := y ^ x;
    return a ^ b;
}

func test(x: Float, y: Float) -> Float {
    a := x ^ y;
    b := x ^ (1.0/y);
    return a + b;
}
