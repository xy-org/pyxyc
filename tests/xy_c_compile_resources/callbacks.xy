struct Callback~[TagCtor{label="cbLabel"}] {}

def test() -> int {
    cb1 := def abs(:int)->int;
    #cb2 := def~Callback(:int)->int;
    #cb3 : (:int, :long)->int;
    #cb3 = def (:int)->int;
    #return cb(5);
}

def abs~Callback(a: int) -> int {
    return if (a < 0) -a else a;
}

