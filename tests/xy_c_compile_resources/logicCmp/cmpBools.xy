def test(a: Bool, b: Bool) {
    c := a == b;
    d := a != b;
    e := !a;
    f := !!a;
    g := a > b;
    h := a >= b;
    i := a < b;
    j := a <= b;
    k := a & b;
    l := a | b;
    m := cmp(a, b);
}