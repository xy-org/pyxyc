def simpleDoWhile(x: Int, y: Int) -> Int {
    i : mut = 0;
    do {
        x += 10;
        i++;
    } while (x < y);
    return i;
}

def write(lim: Int) -> Int {
    return do -> (total := 0) {
        total += step();
    } while (total < lim);
}

def step() -> Int {
    return 10;
}