func cond1(a: Int, b: Int) -> Bool
>> a >= b
{
    return a >= b * 2;
}

func cond2(a: Int, b: Int) -> Bool
>> a < b
{
    return b < 10;
}

struct TypeWithDtor{
    id: Int;
}

func dtor(resource: TypeWithDtor) {
    # ...
}

func test(x: mut Int, y: mut Int) {
    do {
        a := TypeWithDtor{-1};
        x--;
        do {
            b := TypeWithDtor{-2};
            y++;
            if (y > 0) {
                continue outer;
            }
            elif (y < -500) {
                break;
            }
        } while inner(cond2(y, x));
        y--;
    } while outer(cond1(x, y));
}
