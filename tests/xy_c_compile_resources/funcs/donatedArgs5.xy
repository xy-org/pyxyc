struct WithoutDtor {
    val: Int;
}

func action(a: WithoutDtor, b: don Any, ptr: Ptr = b'addrof) {
    # ...
}

func test() {
    without := WithoutDtor{-1};
    action(without, WithoutDtor{});
    action(without, WithoutDtor{10});
    action(without, WithoutDtor{20});
}
