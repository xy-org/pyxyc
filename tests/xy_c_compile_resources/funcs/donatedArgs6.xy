struct WithDtor {
    id: Int;
}

func dtor(val: WithDtor) {
    # ...
}

func action(a: WithDtor, b: don WithDtor, ptr: Ptr = b'addrof) {
    # ...
}

func test() {
    val := WithDtor{-1};
    action(val, WithDtor{});
    action(val, WithDtor{10});
    action(val, WithDtor{20});
}
