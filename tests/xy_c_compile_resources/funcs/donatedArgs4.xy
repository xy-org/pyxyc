# test modifying a donated value with dtor

def modifyFunc(a: don Int) {
    a++;
}

def modifyMacro(a: don Int) = -> void {
    a++;
};

def testFunc() {
    modifyFunc(0);
    modifyFunc(5 + 10);
}

def testMacro() {
    modifyMacro(0);
    modifyMacro(5 + 10);
}
