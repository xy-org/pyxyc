# test modifying a donated value with dtor

func modifyFunc(a: don Int) {
    a++;
}

func modifyMacro(a: don Int) = -> void {
    a++;
};

func testFunc() {
    modifyFunc(0);
    modifyFunc(5 + 10);
}

func testMacro() {
    modifyMacro(0);
    modifyMacro(5 + 10);
}
