struct Pair {
    a : int;
    b : int;
}


def rng() -> int {
    return 0;
}

def myadd(a: int, b: int) -> long {
    return 0l;
}

def ctorPair() -> Pair {
    return Pair{0, 1};
}

def transform(num : long) -> float{
    return 0f;
}

def func() -> void {
    a := rng();
    b := rng() + 10;
    c := (a \myadd b)'transform;
    d := ctorPair();
    e := 1 + 1.1718;
    ef := 2.1718f;
    pi := 1 + 1.0 + 1.14f;
}