struct Pair {
    a : Int;
    b : Int;
}


def rng() -> Int {
    return 0;
}

def myadd(a: Int, b: Int) -> Long {
    return 0l;
}

def ctorPair() -> Pair {
    return Pair{0, 1};
}

def transform(num : Long) -> Float{
    return 0f;
}

def func() -> void {
    a := rng();
    b := rng() + 10;
    c := (a \myadd b)'transform;
    d := ctorPair();
    e := 1 + 1.1718d;
    efp := 2.1718f;
    pi := 1 + 1.0d + 1.14f;
}
