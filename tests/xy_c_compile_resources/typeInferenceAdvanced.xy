struct Pair {
    a : Int;
    b : Int;
}


func rng() -> Int {
    return 0;
}

func myadd(a: Int, b: Int) -> Long {
    return 0l;
}

func ctorPair() -> Pair {
    return Pair{0, 1};
}

func transform(num : Long) -> Float{
    return 0f;
}

func test() -> void {
    a := rng();
    b := rng() + 10;
    c := (a \myadd b)'transform;
    d := ctorPair();
    e := 1 + 1.1718d;
    efp := 2.1718f;
    pi := 1 + 1.0d + 1.14d;
}
