Real := Double;

func r(x: Any) = x'to(Real);

func sqr(x: Real) -> Real {
    return x * x;
}

func test(x: Real) -> Real {
    a : Real = 3.14'r;
    b := 3.14d'r;
    c := 10'r;
    return x * (a / c) * b;
}