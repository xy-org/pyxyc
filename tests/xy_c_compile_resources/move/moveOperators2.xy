struct IntList {
    # ...
}

def get(arr: IntList, i: Int) -> in(arr) Ptr~Int {
    return Ptr{};
}

def set(arr: mut IntList, i: Int, val: Int) -> void {
    # ...
}

def test(a: mut IntList, b: mut IntList, i: Int, j: Int) {
    a[i] =< b[j];
}