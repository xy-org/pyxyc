struct Error {
    code: Int;
}

func to(err: Error, :Bool) err.code != 0;

func errorProne(a: Int) -> Int | Error {
    if (a < 0) {
        error Error{10};
    }
    return 0;
}

func test(var: Int) {
    _ = errorProne(var - 1);
}
