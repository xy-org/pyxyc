func test1(x: Int, y: %x) {
    a := x + y;
    b := x - y;
    c := x * y;
    d := x / y;
    e := x \rem y;
}

func test2(x: Byte, y: %x) {
    a := x + y;
    b := x - y;
    c := x * y;
    d := x / y;
    e := x \rem y;
}

func test3(x: Ushort, y: %x) {
    a := x + y;
    b := x - y;
    c := x * y;
    d := x / y;
    e := x \rem y;
}

func test4(x: Float, y: %x) {
    a := x + y;
    b := x - y;
    c := x * y;
    d := x / y;
}
