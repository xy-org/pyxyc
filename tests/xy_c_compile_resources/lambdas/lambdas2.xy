func inner(x: (Int, Long)) {
    # ...
}

func test1(x: Int) {
    inner(10);

    func inner(x: Int = 20) {
        # ...
    }

    inner();
    inner(20l);
    inner(20);
    inner(x);
}

func test2(x: Int) {
    inner(x);
}
