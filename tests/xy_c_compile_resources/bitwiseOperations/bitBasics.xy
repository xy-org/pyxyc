def test() -> Uint {
    ;; Let's try to blend letters and numbers

    a: mut = Bits32{0xABCDu};
    b: mut = Bits32{0x1234u};

    c := a | b;
    d := a & b;
    e := a - b;

    # f := !(c | d & e);

    return e.value;
}

