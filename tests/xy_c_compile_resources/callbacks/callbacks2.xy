struct Test~[xyTag:=TagCtor{label="test"}] {}

func test1~Test() {}

func test2~Test() {}

func invokeTests() {
    tests := @for (f in $* ~Test()) f'toCallback;
    for (f in tests) {
        [f]();
    }
}

func test3~Test() {}