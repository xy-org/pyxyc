def test() {
    arr : @Int[20];
    f(arr);
    g(arr);
}

def f(arr: pseudo @Int[], ptr := arr'addrof, len := arr'len) {
    # ...
}

def g(arr: pseudo @Any[], ptr: Ptr = arr'addrof, len := arr'len) {
    # ...
}
