func test() {
    arr : @Int[20];
    f(arr);
    g(arr);
}

func f(arr: pseudo @Int[], ptr := arr'addrof, len := arr'len) {
    # ...
}

func g(arr: pseudo @Any[], ptr: Ptr = arr'addrof, len := arr'len) {
    # ...
}
