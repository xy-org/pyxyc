import libc~[Clib{headers=@{"stddef.h"}}] in c;

struct Data {
    ext := c.ptrdiff_t{0},
    a: Int,
}

struct TypeInferenceData {
    ext1 := c.ptrdiff_t{20},
    ext2 := c.ptrdiff_t{0},
}

func test() {
    data := Data{a=10};
    tiData1 := TypeInferenceData{};
    tiData2 := TypeInferenceData{ext2=c.ptrdiff_t{30}};
}
