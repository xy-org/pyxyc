import posix~[Clib{headers=@{"termios.h", "unistd.h"}}] in c;

struct Data {
    num: Int,
    ext := c."struct termios"{0},
    flt: Float,
}

func test() {
    a := Data{};
    b := Data{flt=2.0};
}