struct Complex {
    # fields ...
    this := 0,
    that := 10,
}

func dtor(c: Complex) {
    # ...
}

func setupThis(c: mut Complex, val: Int) {
    c.this = val;
}

func setupThat(c: mut Complex) {
    c.that += c.this;
}

func test() {
    a := Complex{
        5,
        that=20,
        'setupThis(50),
        'setupThat,
    };
}
