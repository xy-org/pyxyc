struct Str {
    data: Ptr~Byte;
    size: Usize;
}

func dtor(s: Str) {
        # ...
}

func str~[StrCtor{prefix=""}](data: Ptr, len: Usize) Str{data, len};

struct Log {
    level: Int;
}

*func info(^msg: Str, log : Log) =
if (log.level <= 0) {
    write(^msg);
};

func write(msg: Str) {
    # ...
}

func test() {
    info("message", Log{});
}
