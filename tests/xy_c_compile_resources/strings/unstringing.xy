struct Str {
    addr: Ptr~Byte;
    size: Usize;
}

def str~[StrCtor{}](addr: Ptr~Byte, len: Usize) = Str{addr, len};

def unstr~[UnstrCtor{}](str: Str) = 0uz;

def read(s: Str, i: mut Usize, val: mut Int) {
    # ...
}

def test() {
    "{a: Int}{b: Int}" = "123 456";
}

def test2(s: Str) {
    "{a: Int}{b: Int}" = s;
}
