struct Str~[encoding: Any = Utf8] {
    addr: Ptr;
    size: Usize;
}

struct Utf8~[xyTag := TagCtor{label="encoding"}] {}
struct Ascii~[xytag := TagCtor{label="encoding"}] {}

func str~[StrCtor{prefix="", interpolation=false}](
    addr: Ptr, size: Usize
) -> Str {
    return Str{addr, size};
}

func createStrings() -> void {
    str := "";
    str1 := "abc";
    str2 := "def";
}

func strLen(str: Str) -> Usize {
    return str.size;
}
