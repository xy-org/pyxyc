struct Str~[encoding: Any = Utf8] {
    addr: Ptr;
    size: Usize;
}

struct Utf8~[xyTag := TagCtor{label="encoding"}] {}
struct Ascii~[xytag := TagCtor{label="encoding"}] {}

def str~[StrCtor{prefix="", interpolation=false}](
    addr: Ptr, size: Usize
) -> Str {
    return Str{addr, size};
}

def createStrings() -> void {
    str := "";
    str1 := "abc";
    str2 := "def";
}

def strLen(str: Str) -> Usize {
    return str.size;
}
