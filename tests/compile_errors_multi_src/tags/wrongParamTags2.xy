struct Str {
    addr: Ptr;
    size: Size;
}

func f1(s: Str) = s..hash;

func test() -> Int {
    s1: Str;
    s2: Str~[hash=10];
    y := f1(s2);
    x := f1(s1); # error as s1 doesn't have a hash tag
    return x + y;
}