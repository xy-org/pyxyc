;; Test passing a reference to a list and an item to that list at the same time

struct IntList {
    # ...
}

func get(arr: IntList, i: Int) -> arr[ Ptr~Int ] {
    return Ptr{};
}

func set(arr: mut IntList, i: Int, val: Int) -> void {
    # ...
}

func fun(item: mut Int, list: mut IntList) {}

func test(a: mut IntList, b: mut IntList) {
    fun(a[0], b);; not safe as a and b may alias
}