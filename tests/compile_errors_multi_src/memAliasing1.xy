;; Test passing a reference to a list and an item to that list at the same time

struct List~[type: struct] {
    # ...
}

func get(arr: List, i: Int) -> arr[ Ptr~[^arr..type] ] {
    return Ptr{};
}

func set(arr: mut List, i: Size, val: pseudo Any) -> void {
    # ...
}

func fun(item: mut Int, list: mut List~Int) {}

func test() {
    a: List~Int;
    fun(a[0], a);; should give an error.
}
