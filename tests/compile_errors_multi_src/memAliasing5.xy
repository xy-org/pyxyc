struct Point {
    x: Int;
    y: Int;
}

struct PointList {
    # ...
}

func get(arr: PointList, i: Int) -> arr[ Ptr~Point ] {
    return Ptr{};
}

func set(arr: mut PointList, i: Int, val: Point) -> void {
    # ...
}

func fun(item: Int, list: mut Point) {}

func test(a: mut PointList) {
    fun(a[0].y, a[1]);
}
