def swap(a: inout int) {
    # should produce an error
    doSwap(a, a);
}

def doSwap(a: inout int, b: inout int) {
    tmp := a;
    a = b;
    b = a;
}

def test() {
    a : mut = 0;
    b : mut = 1;
    swap(a);
    swap(b);
}