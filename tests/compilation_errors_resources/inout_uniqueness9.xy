func swap(a: mut Int) {
    # should produce an error
    doSwap(a, a);
}

func doSwap(a: mut Int, b: mut Int) {
    tmp := a;
    a = b;
    b = a;
}

func test() {
    a : mut = 0;
    b : mut = 1;
    swap(a);
    swap(b);
}